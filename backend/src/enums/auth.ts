export enum TokenType {
	ACCESS_TOKEN = "ACCESS_TOKEN",
	REFRESH_TOKEN = "REFRESH_TOKEN"
}

export enum CookieTokenType {
	ACCESS_TOKEN = "accessToken",
	REFRESH_TOKEN = "refreshToken"
}

export enum SameSiteCookieOption {
	STRICT = "strict",
	LAX = "lax",
	NONE = "none",
}