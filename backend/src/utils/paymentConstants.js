export const Currencies = Object.freeze({
	USD: "usd"
});

export const PaymentMethodTypes = Object.freeze({
	CARD: "card"
});

export const CheckoutSessionModes = Object.freeze({
	PAYMENT: "payment"
});

export const PaymentStatus = Object.freeze({
	PAID: "paid"
});