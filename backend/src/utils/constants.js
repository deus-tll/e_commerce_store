export const FileFolders = Object.freeze({
	PRODUCTS: "products",
	CATEGORIES: "categories"
});

export const TokenTypes = Object.freeze({
	ACCESS_TOKEN: "ACCESS_TOKEN",
	REFRESH_TOKEN: "REFRESH_TOKEN"
});

export const CookieTokenTypes = Object.freeze({
	ACCESS_TOKEN: "accessToken",
	REFRESH_TOKEN: "refreshToken"
});

export const SameSiteCookieOptions = Object.freeze({
	STRICT: "strict",
	LAX: "lax",
	NONE: "none",
});

export const CacheKeys = Object.freeze({
	FEATURED_PRODUCTS: "featured_products",
	REFRESH_TOKEN: "refresh_token"
});

export const UserRoles = Object.freeze({
	CUSTOMER: "customer",
	ADMIN: "admin"
});

export const EnvModes = Object.freeze({
	PROD: "production",
	DEV: "development"
});

export const ServerPaths = Object.freeze({
	STATIC_URL_PREFIX: "/public",
	STATIC_FOLDER_NAME: "../public",
});

export const BASE64_PREFIX = "data:";