apiVersion: apps/v1
kind: Deployment
metadata:
  name: local-redis-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: local-redis
  template:
    metadata:
      labels:
        app: local-redis
    spec:
      containers:
      - name: redis
        image: redis:latest
        ports:
        - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: local-redis-service
spec:
  selector:
    app: local-redis
  ports:
    - protocol: TCP
      port: 6379
      targetPort: 6379